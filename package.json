{
  "name": "pong-multiplayer",
  "version": "0.0.1",
  "scripts": {
    "build": "esbuild --platform=node --bundle --sourcemap --outfile=build/index.cjs index.ts",
    "dev": "npm run build -- --watch=forever & nodemon -w build build/index.cjs"
  },
  "description": "multiplayer pong",
  "dependencies": {
    "@types/express": "^4.17.17",
    "esbuild": "^0.17.19",
    "express": "^4.18.2",
    "socket.io": "^4.6.2"
  },
  "devDependencies": {
    "source-map-support": "^0.5.21"
  }
}
